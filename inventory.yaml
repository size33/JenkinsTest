all:
  children:
    jenkins:
      hosts:
        3.9.245.41:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: "~/.ssh/linux.pem"
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
